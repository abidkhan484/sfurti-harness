/** Concrete local extraction adapter; kept separate from HTTP collection. */
export {
  DeterministicExtractor as LocalDocumentExtractor,
  type ExtractionCoverage,
  type ExtractionMetadata,
  type ExtractionResult,
  type ExtractorConfig,
  type ExtractRequest,
  type FeedEntry,
} from "../../research/extraction.ts";
